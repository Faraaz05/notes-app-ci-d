# Node modules should NEVER be copied; Docker will install deps separately
node_modules
npm-debug.log
yarn-debug.log
yarn-error.log

# Environment files (never include secrets in images)
.env
.env.* 

# Build artifacts or logs
*.log
logs/
dist/
coverage/

# Git
.git
.gitignore

# OS files
.DS_Store
Thumbs.db

# Editor folders
.vscode
.idea

# Docker files (You don’t want them copied inside the image)
Dockerfile
docker-compose.yml
